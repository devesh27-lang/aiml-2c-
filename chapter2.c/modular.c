#include<stdio.h>    //modular operator returns remainder for int
int main() {
// not work on float value works only on integers
    printf("%d \n" , 16%10);
    printf("%d", -8%3);
    return 0;
} 
  