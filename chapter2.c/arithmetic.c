#include<stdio.h>
#include<math.h>
int main() {

    int a=1,b=2;
    int sum = a + b;
    int multiply = a * b;
    int power = pow(b,a);
    printf("sum = %d\n",sum);
    printf("multiply = %d\n",multiply);
    printf("%d",power);
  return 0;
} 
